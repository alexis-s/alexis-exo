{

  cout << "this is alexis' rootlogon.C" << endl;

  gROOT->SetStyle("Plain");
  gStyle->SetOptStat(0);
  gStyle->SetPalette(1);

  // use consistent number of significant digits:
  gStyle->SetStripDecimals(kFALSE);

}
